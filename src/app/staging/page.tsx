export default function StagingPage() {
  return (
    <main className="min-h-screen bg-[hsl(var(--game-cream))] px-6 py-12">
      <div className="mx-auto max-w-4xl">
        <h1 className="font-heading text-4xl font-black text-[hsl(var(--game-ink))]">
          Component Library
        </h1>
        <p className="mt-2 font-heading text-sm text-[hsl(var(--game-ink-light))]">
          Idle Hours design system — staging only
        </p>
        <hr className="mt-6 border-[hsl(var(--game-ink))]/10" />

        {/* Components will be added here */}
      </div>
    </main>
  )
}
