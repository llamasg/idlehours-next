import {defineConfig} from 'vite'

// Sanity reads Vite config from the `vite` callback in sanity.cli.ts.
// This file exists only as a fallback for standalone Vite tooling.
export default defineConfig({})
