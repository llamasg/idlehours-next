/**
 * Pip — Research Module
 *
 * Fetches data from:
 *   - Google Analytics 4 (sessions, top pages, traffic sources)
 *   - Google Search Console (queries, positions, CTR)
 *
 * Returns a structured research object that generate.js uses as context.
 */
