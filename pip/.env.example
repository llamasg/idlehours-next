# ── ANTHROPIC ────────────────────────────────────────────────
# Get from: console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ── SANITY ───────────────────────────────────────────────────
# Get from: sanity.io/manage → your project
SANITY_PROJECT_ID=
SANITY_DATASET=production
# Write token — needs Editor or higher permissions
# Get from: sanity.io/manage → your project → API → Tokens
SANITY_WRITE_TOKEN=

# ── GOOGLE ANALYTICS 4 ───────────────────────────────────────
# Property ID — found in GA4 Admin → Property Settings
# Format: numeric only e.g. 123456789
GA4_PROPERTY_ID=

# Path to service account JSON key file (for local dev)
# For GitHub Actions, use GOOGLE_SERVICE_ACCOUNT_JSON instead
GOOGLE_APPLICATION_CREDENTIALS=./google-service-account.json

# ── GOOGLE SEARCH CONSOLE ────────────────────────────────────
# Site URL exactly as it appears in Search Console
# Usually: https://idlehours.co.uk/ (with trailing slash)
SEARCH_CONSOLE_SITE_URL=

# ── SITE CONFIG ──────────────────────────────────────────────
# Active cluster name — must match exactly what's in pipClusters
PIP_ACTIVE_CLUSTER=Anxiety & Low Energy

# Run in dry mode (generates but doesn't write to Sanity)
PIP_DRY_RUN=false
